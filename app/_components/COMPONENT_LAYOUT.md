ui: re-usable components (e.g., button, input, modal, card, badge)

layout: components used across pages (e.g., sidebar, Header)

features: components and logic specific to a page (e.g., projectList, projectFilters, tagList, StatsCards)
